import { Fragment } from "react";
import SignInForm from "../components/auth/SignInForm";

const LogIn = (props) => {
  return (
    <Fragment>
      <SignInForm />
    </Fragment>
  );
};

export default LogIn;

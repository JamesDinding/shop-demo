import classes from "./Footer.module.css";

const Footer = (props) => {
  return (
    <footer className={classes.footer}>
      <div>This is react demo created by DinDing James</div>
    </footer>
  );
};

export default Footer;
